import styled from "styled-components";

const Message = styled.h1``;

const RequestLocationPermission = () => {
  return <Message>位置情報の取得を許可するアル</Message>;
};

export default RequestLocationPermission;
